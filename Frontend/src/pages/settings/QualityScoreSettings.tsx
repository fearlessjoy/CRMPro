
import { SettingsPageTemplate } from "@/components/settings/SettingsPageTemplate";

const QualityScoreSettings = () => {
  return (
    <SettingsPageTemplate
      title="Define Quality Score"
      description="Configure quality scoring parameters"
    >
      <p>Quality score configuration will go here.</p>
    </SettingsPageTemplate>
  );
};

export default QualityScoreSettings;

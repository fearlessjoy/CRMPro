
import { SettingsPageTemplate } from "@/components/settings/SettingsPageTemplate";

const CampaignSettings = () => {
  return (
    <SettingsPageTemplate
      title="Abroad Studies Pages Campaign"
      description="Configure campaign settings for abroad studies pages"
    >
      <p>Campaign settings configuration will go here.</p>
      {/* Content for the Campaign Settings page */}
    </SettingsPageTemplate>
  );
};

export default CampaignSettings;

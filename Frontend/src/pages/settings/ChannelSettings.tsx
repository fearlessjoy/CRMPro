
import { SettingsPageTemplate } from "@/components/settings/SettingsPageTemplate";

const ChannelSettings = () => {
  return (
    <SettingsPageTemplate
      title="Abroad Channel Settings"
      description="Configure channel settings for abroad programs"
    >
      <p>Channel settings configuration will go here.</p>
      {/* Content for the Channel Settings page */}
    </SettingsPageTemplate>
  );
};

export default ChannelSettings;

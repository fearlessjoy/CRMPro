
import { SettingsPageTemplate } from "@/components/settings/SettingsPageTemplate";

const LoginDetailsSettings = () => {
  return (
    <SettingsPageTemplate
      title="Subcrm Login/Logout Details"
      description="View and manage login activity"
    >
      <p>Login details configuration will go here.</p>
    </SettingsPageTemplate>
  );
};

export default LoginDetailsSettings;

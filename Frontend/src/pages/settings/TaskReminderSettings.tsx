
import { SettingsPageTemplate } from "@/components/settings/SettingsPageTemplate";

const TaskReminderSettings = () => {
  return (
    <SettingsPageTemplate
      title="Task Reminder Checklist"
      description="Configure task reminder settings"
    >
      <p>Task reminder configuration will go here.</p>
    </SettingsPageTemplate>
  );
};

export default TaskReminderSettings;

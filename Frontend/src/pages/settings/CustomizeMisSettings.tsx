
import { SettingsPageTemplate } from "@/components/settings/SettingsPageTemplate";

const CustomizeMisSettings = () => {
  return (
    <SettingsPageTemplate
      title="Customize MIS"
      description="Configure management information system reports"
    >
      <p>MIS customization configuration will go here.</p>
    </SettingsPageTemplate>
  );
};

export default CustomizeMisSettings;
